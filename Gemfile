source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in sql_view.gemspec.
gemspec

gem "pg"
gem "pry"
gem "wrapped_print"
gem "scenic"

# Start debugger with binding.b [https://github.com/ruby/debug]
# gem "debug", ">= 1.0.0"
